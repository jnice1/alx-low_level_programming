#include <stdio.h>
#include <stdlib.h>
/**
 *main - Entry point
 *
 *Return: Always 0 (success)
 *
 */
int main(void)
}
int i;
char c = 'a';
while (i <= 9)
{
putchar(i);
i++;
}
while (c <= 'f')
{
putchar(c)
c++
}
putchar('\n')
}
return (0);
}
