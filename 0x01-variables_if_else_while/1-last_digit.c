#include <stdio.h>
#include <stdlib.h>
/**
* main - entry point
* 
* Return: Always 0 (success)
*
*/
int main(void)
{
char low;
for (low = 'a'; low <=z; low++);
put char(low);
put char('\n');
return (0);
}
